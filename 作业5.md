# 作业五

## 本次大作业是自己制作一个vibe coding项目

我选择的是增加AI查询功能的游戏助手。我最近玩《明日方舟终末地》时，对流水线加工类游戏感兴趣了，因此玩了一下戴森球计划。

戴森球计划前期的图很复杂，流水线步骤多。因此选择了一个前期发展的助手，功能以查询配方，AI助手提供优化策略为主。

## 使用的vibe coding提示词

请在一个 Vercel + React(Vite) 项目中实现 DSP 配方查询/解释 Demo。

数据文件：
- public/data/v0.json
- public/data/data_items.json

运行时合并规则：
1) data_items.json 覆盖 v0.items[].Name（名称最终以 data_items 为准）。
2) 若 v0.items 缺少某 itemId 但 recipes 引用了它：仍保留该物品（至少提供 Name；IconName/Type/GridIndex 可空）。
3) 默认禁用 “无中生有” 配方：任何 recipe.Type == -1 一律过滤不可用。

后端（Vercel Functions，api/）：
- 实现 DataStore（api/_datastore.ts）：
  - 加载 v0 与 data_items（fetch /data/*.json）
  - 构建 itemsById、recipesById、recipesByResult、nameToItemIds
  - 提供 searchItems、getItem、getRecipesProducing、expandDependencies(depth) 等
  - 启动时做基本校验（Counts 长度一致、索引有效等）

提供 API：
- GET /api/search?q=
- GET /api/item?id=
- GET /api/recipes-producing?itemId=
- GET /api/deps?itemId=&depth=
- POST /api/chat  (调用 deepseek-chat；key 来自环境变量 DEEPSEEK_API_KEY)
  - chat 的策略：后端先用 DataStore 查出相关物品/配方/依赖树摘要，再把摘要+用户问题发给 LLM，让 LLM 负责中文解释；禁止 LLM 凭空编配方。

前端（React/Vite）：
- 搜索框（调用 /api/search）
- 物品详情（/api/item）
- 配方列表（/api/recipes-producing）
- 依赖树视图（/api/deps，depth 可调）
- 聊天面板（/api/chat）

交付：
- 本地可运行与 Vercel 可部署（README）
- 完成搜索->查看配方->依赖树->聊天解释闭环

## 作业视频

![演示视频](https://github.com/yifeishiny/ai_learning_note/blob/easy_code/%E6%88%B4%E6%A3%AE%E7%90%83%E8%AE%A1%E5%88%92%E5%8A%A9%E6%89%8B2.mp4)

